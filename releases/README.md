Place the signed release DPC APK here (see `../dpc-app/README.md`), e.g.:

```
autosetup-dpc-1.0.0.apk
checksums.json
```

This directory is gitignored for `*.apk` — the APK is a release artifact,
not source.
